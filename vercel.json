{
  "buildCommand": "npm install --prefix client && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/((?!api/|assets/|favicon.ico).*)", "destination": "/index.html" }
  ]
}
